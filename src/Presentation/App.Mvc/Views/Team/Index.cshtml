@using App.Domain.Entities
@using App.Infra.Implementation.Filter
@model Paginate<ProjectTeamEntity>

<div class="page-header d-print-none mb-5">
    <div class="container-xl">
        <div class="row g-2 align-items-center">
            <div class="col">
                <h2 class="page-title">
                    Teams
                </h2>
                <small class="pt-1">Showing @Model.StartIndex to @Model.EndIndex of @Model.Total entries</small>
            </div>
            <div class="col-auto ms-auto d-print-none">
                <div class="btn-list">
                    <a asp-controller="Team" asp-action="Create" class="btn btn-success d-none d-sm-inline-block">
                        <i class="ti ti-book-upload"></i>
                        Create a new team
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container-xl">
    <div class="row g-4">
        <div class="col-12">
            <form asp-controller="Team" asp-action="Index" method="get">
                <partial name="_FilterPartial" model="Model" />
            </form>
        </div>
        <div class="col-md-12">
            <partial name="_TeamPartial" model="Model" />
        </div>
        <div class="col-12">
            <div class="card bg-transparent border-0">
                <div class="card-body">
                    <partial name="_PaginationPartial" model="Model" />
                </div>
            </div>
        </div>
    </div>
</div>