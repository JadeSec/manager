@using App.Domain.Entities
@using App.Infra.Implementation.Filter
@model Paginate<ProjectOrganizationEntity>

<style>
    .org .avatar {
        --tblr-avatar-size: 3.5rem !important;
    }

    .org .font-weight-medium {
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .org .text-secondary {
        overflow: hidden;
        text-overflow: ellipsis;
        display: -webkit-box;
        -webkit-box-orient: vertical;
        -webkit-line-clamp: 2; /* number of lines to show */
        line-height: 1.2; /* fallback */
        margin-top: 2.5px;
    }
</style>

<div class="row row-cards">
    @foreach (var org in Model.Items)
    {

    <div class="col-md-6 col-xl-3">       
        <a class="org card card-link shadow border-0" href="#">
            <div class="card-body">
                <div class="row">
                    <div class="col-auto">
                        <span class="avatar rounded" style="background-image: url(@org.Icon)"></span>
                    </div>
                    <div class="col">
                        <div class="font-weight-medium fw-bold">@org.Name</div>
                        <div class="text-secondary">@org.Description</div>
                    </div>
                </div>
            </div>
        </a>
    </div>
    }
</div>